<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="crm_stage_form_inherit" model="ir.ui.view">
        <field name="name">crm.stage.form</field>
        <field name="model">crm.stage</field>
        <field name="inherit_id" ref="crm.crm_stage_form"/>
        <field name="arch" type="xml">
            <group position="after">
                <group name="automatic_reminders" string="Automatic reminders">
                    <field name="send_automatic_reminder" widget="boolean_toggle"/>
                    <field name="lead_mail_rule_ids" attrs="{'invisible': [('send_automatic_reminder', '=', False)]}">
                        <tree editable="bottom">
                            <field name="email_template_id" />
                            <field name="name"/>
                            <field name="send_reminder_after" width="200px"/>
                        </tree>
                    </field>
                </group>
            </group>
        </field>
    </record>
</odoo>